<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.requisitos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Requisitos">
        <activity
            android:name=".ListaRequisitosactivity"
            android:label="@string/title_activity_lista_requisitosactivity"
            android:theme="@style/Theme.Requisitos.NoActionBar"></activity>
        <activity
            android:name=".ListaProjetosactivity"
            android:label="@string/title_activity_lista_projetosactivity"
            android:theme="@style/Theme.Requisitos.NoActionBar" />
        <activity
            android:name=".CadastroRequisitosActivity"
            android:label="@string/title_activity_cadastro_requisitos"/>
        <activity
            android:name=".CadastroProjetoActivity"
            android:label="@string/title_activity_cadastro_projeto" />
        <activity
            android:name=".MenuActivity"
            android:exported="true"
            android:theme="@style/Theme.Requisitos.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>